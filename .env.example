# CLAP2Diffusion Environment Variables
# Copy this file to .env and adjust values as needed

# GPU Configuration
CUDA_VISIBLE_DEVICES=0
NVIDIA_VISIBLE_DEVICES=all
NVIDIA_DRIVER_CAPABILITIES=compute,utility

# Gradio Configuration
GRADIO_SERVER_NAME=127.0.0.1  # Use 0.0.0.0 to expose publicly (security risk!)
GRADIO_SERVER_PORT=7860
GRADIO_USERNAME=admin
GRADIO_PASSWORD=clap2diffusion  # Change this in production!

# Model Cache
MODEL_CACHE_DIR=/app/models
HF_HOME=/app/.cache/huggingface

# Data Paths (for local development)
AUDIOCAPS_ROOT=./data/audiocaps
AUDIOCAPS_METADATA=./data/audiocaps/metadata.json

# Training Configuration (optional)
WANDB_API_KEY=
WANDB_PROJECT=clap2diffusion

# Security Note:
# - Always change default passwords
# - Use GRADIO_SERVER_NAME=127.0.0.1 for local access only
# - Consider using a reverse proxy with SSL for production